name: "Greeter Action"
description: "Print a greeting message"
inputs:
  who-to-greet:
    description: "Who to greet"
    required: false
    default: "World"
runs:
  using: "composite"
  steps:
    - run: echo "Hello ${{ inputs.who-to-greet }}!"
      shell: bash
